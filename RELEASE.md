## v1.0.4

### 新增

- **外部接收**：支持从其他 App 分享截图/文本直接识别，适配 vivo 原子岛 / OPPO 流体云拖放直达
- **地图验证**：提取地址后自动调用地理编码验证真实性（支持高德 API），详情页显示验证徽标
- **自学习闭环**：从未识别样本中自动聚类发现新模式，自动生成正则并应用
- **统计面板**：展示识别命中率、模式分布、已学习规则、候选模式建议
- **去重重写**：同码记录批量清理，一次操作清理所有重复；重复出现时推送通知
- **来源识别升级**：订单号前缀匹配优先于 OCR 文本（JT→极兔、SF→顺丰等）；品牌+后缀结构匹配

### 修复

- OCR 横杠归一化：Unicode 破折号变体自动转为 ASCII `-`，解决 `D—06003` 无法匹配的问题
- 新增 `D06003` 等字母+数字取件码格式支持（韵达/邮政）
- 地址提取收紧：必须含路/街/号/柜等地址标识符，避免误提取快递描述文本
- 来源品牌误报修复：`【周口市】` 不再被当作来源；`申请通过` 不再匹配 `申通`

### 技术

- 数据库升级 v3（新增地图验证字段）
- 自学习系统：PatternLearner 聚类分析 + 自动正则生成 + 持久化已学习规则
- 快递100接口预留（待运单号输入方案）
- 新增文件：StatsScreen、GeocoderVerifier、Kuaidi100Verifier、PatternLearner、ShareReceiver、IntentHandler
